"""
config.py — 전역 설정 및 튜닝 파라미터 (팀원 필독)

시스템 전반에서 사용하는 상수를 한 곳에 모아둡니다. 값을 바꿔야 할 때
이 파일 하나만 수정하면 되도록 설계되었습니다.
"""
import os

# ------------------------------------------------------------------ #
# 백엔드 API
# ------------------------------------------------------------------ #
API_BASE_URL = os.getenv("API_BASE_URL", "http://127.0.0.1:8000")
API_URL = API_BASE_URL + "/detect"

# ------------------------------------------------------------------ #
# 클래스 정의
# ------------------------------------------------------------------ #
PERSON_CLASSES = {"사람"}

# ------------------------------------------------------------------ #
# 시스템 과부하 및 알람 도배를 막기 위한 핵심 튜닝 값입니다. 운영 환경에 맞춰 조절하세요.
# ------------------------------------------------------------------ #
DETECT_INTERVAL = 4            # N 프레임마다 한 번만 백엔드 API에 추론을 요청합니다. (나머지 프레임은 박스 유지)
PERSON_GAP_TOLERANCE = 10      # 화면에서 사람이 사라져도 N 프레임 동안은 추적을 끊지 않고 대기합니다. (깜빡임 방지)
ANIMAL_TOAST_COOLDOWN = 10     # 같은 동물이 계속 뜰 때, 알람이 도배되지 않도록 N초 동안 추가 알림을 무시합니다.
AUTO_POPUP_COOLDOWN = 15       # 메인 경보 팝업이 한 번 뜨면 N초 동안은 새 팝업을 띄우지 않습니다.

# 신뢰도 임계값(conf_thresh)의 단일 fallback 지점입니다.
# 정상적인 경우 conf_thresh는 항상 backend.py의 응답(conf_thresh_used)에서 가져오며,
# 데모 모드이거나 백엔드 호출이 실패했을 때만 이 값이 사용됩니다.
# (데모 모드를 제거하더라도 "백엔드 호출 실패 시" fallback으로 계속 쓰이므로 삭제하지 마세요.)
FALLBACK_CONF_THRESH = 0.5
FALLBACK_NMS_THRESH = 0.5

# ------------------------------------------------------------------ #
# 화면 표시
# ------------------------------------------------------------------ #
COLORS = {
    "사람": "#f85149",
    "멧돼지": "#e3a008",
    "고라니": "#3fb950",
}
DEFAULT_COLOR = "#58a6ff"

IMAGE_EXTS = ("jpg", "jpeg", "png")
VIDEO_EXTS = ("mp4", "mov", "avi", "mkv")

CAMERAS = [
    {"id": "cam1", "name": "CCTV-01 (초소 A)"},
    {"id": "cam2", "name": "CCTV-02 (초소 B)"},
    {"id": "cam3", "name": "CCTV-03 (철책 C구간)"},
    {"id": "cam4", "name": "CCTV-04 (감시초소 D)"},
]
