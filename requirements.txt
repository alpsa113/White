# GOP 통합 감시 시스템 — FastAPI 백엔드 의존성 목록 (프론트엔드는 frontend/package.json 참고)
#
# 설치 방법:
#   pip install -r requirements.txt
#
# 주의: opencv-python은 로컬 개발/시연 환경(모니터 있는 PC) 기준입니다.
# 헤드리스 서버(EC2 등 GUI 없는 환경)에 배포할 경우 opencv-python 대신
# opencv-python-headless를 설치하세요 (기능은 동일, 불필요한 GUI 의존성만 제거됨).

# ── FastAPI 서버 (추론 + REST API) ──
fastapi
uvicorn
ultralytics
python-multipart
python-dotenv

# ── 영상 디코딩/처리 ──
numpy
opencv-python
pandas

# ── 탐지 클립(mp4) 인코딩 ──
# imageio-ffmpeg는 플랫폼별 ffmpeg 실행파일을 패키지 안에 내장하고 있어
# 시스템에 별도로 ffmpeg를 설치할 필요가 없습니다.
imageio
imageio-ffmpeg

# ── 공통 ──
pillow

# ── AWS 연동 (RDS/S3) ──
sqlalchemy
pymysql
boto3

dotenv